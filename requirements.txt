opencv-python-headless>=4.8.0
pytesseract>=0.3.10
scikit-image>=0.21.0
scipy>=1.11.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
deepforest>=2.1.0
torch>=2.0.0
torchvision>=0.15.0
huggingface-hub>=0.20.0
PyYAML>=6.0
# Optional: publishes eval results to a Comet dashboard via
# scripts/log_to_comet.py. Imported lazily, so the pipeline and the tests run
# without it.
comet_ml>=3.47.0